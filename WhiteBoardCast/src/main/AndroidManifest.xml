<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.livejournal.karino2.whiteboardcast"
    android:versionCode="7"
    android:versionName="1.7" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="16" />

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        android:debuggable="false"
      debuggable is not becomes true when we release using AndroidStudio.
     I manually turn on for workaround.
    -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.livejournal.karino2.whiteboardcast.WhiteBoardCastActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.livejournal.karino2.whiteboardcast.VideoListActivity"
            android:label="@string/title_video_list" >
        </activity>
        <activity
            android:name="com.livejournal.karino2.whiteboardcast.VideoDetailActivity"
            android:label="@string/title_video_detail"
            android:parentActivityName=".VideoListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".VideoListActivity" />
        </activity>
    </application>

</manifest>
